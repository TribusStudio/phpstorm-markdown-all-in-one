# /test — Run the test suite

Run all tests for the Markdown All-in-One plugin.

## Steps
1. Run `./gradlew test` from the project root
2. If tests fail, analyze failures and report which tests failed and why
3. If all tests pass, report the count of tests run
4. Check for any skipped or ignored tests and report those too
